import { AdminPlaceholder } from "@/components/admin/admin-placeholder";
import { createClient } from "@/lib/supabase/server";
import { deleteSubscriber,sendQueuedNotifications,setSubscriberStatus } from "@/app/admin/subscribers/actions";

export default async function AdminSubscribers(){const supabase=await createClient();const [{data:subscribers},{count:queued}]=await Promise.all([supabase.from("subscribers").select("id,email,name,status,created_at").order("created_at",{ascending:false}),supabase.from("notification_sends").select("id",{count:"exact",head:true}).eq("status","queued")]);const configured=Boolean(process.env.RESEND_API_KEY&&process.env.RESEND_FROM_EMAIL);return <AdminPlaceholder title="Subscribers" description="Manage confirmations, opt-outs, and queued journal notifications."><div className="mt-7 flex flex-wrap items-center justify-between gap-3 border-2 border-forest bg-white p-4"><div><strong>{subscribers?.length??0} subscribers</strong><p className="text-xs text-stone-600">{queued??0} notifications queued · Email {configured?"configured":"not configured"}</p></div><form action={sendQueuedNotifications}><button className="button-primary" disabled={!configured||!queued}>Send queued emails</button></form></div>{!configured&&<p className="mt-4 border-2 border-amber-800 bg-amber-50 p-3 text-sm text-amber-950">Add RESEND_API_KEY and RESEND_FROM_EMAIL to enable confirmation and journal emails.</p>}<div className="mt-5 overflow-x-auto border-2 border-forest bg-white"><table className="w-full min-w-[620px] text-left text-sm"><thead className="bg-forest text-white"><tr><th className="p-3">Subscriber</th><th className="p-3">Status</th><th className="p-3">Joined</th><th className="p-3">Actions</th></tr></thead><tbody>{(subscribers??[]).map((subscriber)=><tr key={subscriber.id} className="border-t border-stone-200"><td className="p-3"><strong>{subscriber.name||"—"}</strong><span className="block text-xs text-stone-600">{subscriber.email}</span></td><td className="p-3 capitalize">{subscriber.status}</td><td className="p-3">{new Date(subscriber.created_at).toLocaleDateString()}</td><td className="flex gap-2 p-3">{subscriber.status!=="active"&&<form action={setSubscriberStatus}><input type="hidden" name="id" value={subscriber.id}/><input type="hidden" name="status" value="active"/><button className="text-xs font-bold text-green-800 underline">Activate</button></form>}{subscriber.status==="active"&&<form action={setSubscriberStatus}><input type="hidden" name="id" value={subscriber.id}/><input type="hidden" name="status" value="unsubscribed"/><button className="text-xs font-bold text-amber-800 underline">Unsubscribe</button></form>}<form action={deleteSubscriber}><input type="hidden" name="id" value={subscriber.id}/><button className="text-xs font-bold text-red-800 underline">Delete</button></form></td></tr>)}</tbody></table></div></AdminPlaceholder>}
