import Link from "next/link";
import { FileText, MessageCircle, Plus, Users } from "lucide-react";
import { AdminPlaceholder } from "@/components/admin/admin-placeholder";
import { createClient } from "@/lib/supabase/server";

export default async function AdminPage(){const supabase=await createClient();const [{count:posts},{count:comments},{count:subscribers}]=await Promise.all([supabase.from("posts").select("id",{count:"exact",head:true}).eq("status","published"),supabase.from("comments").select("id",{count:"exact",head:true}).eq("status","pending"),supabase.from("subscribers").select("id",{count:"exact",head:true}).eq("status","active")]);const stats=[["Published posts",String(posts??0),FileText],["Pending comments",String(comments??0),MessageCircle],["Active subscribers",String(subscribers??0),Users]] as const;return <AdminPlaceholder title="Trip desk" description="A mobile-first overview for publishing from the road."><div className="mt-9 grid gap-4 sm:grid-cols-3">{stats.map(([label,value,Icon])=><div key={label} className="rounded-2xl bg-white p-5 shadow-sm"><Icon className="text-sage" size={20}/><p className="mt-4 font-serif text-3xl font-semibold text-forest">{value}</p><p className="text-sm text-stone-500">{label}</p></div>)}</div><Link href="/admin/posts/new" className="button-primary mt-6 gap-2"><Plus size={17}/>New entry</Link></AdminPlaceholder>}
