import Link from "next/link";
import { createClient } from "@/lib/supabase/server";

export default async function Unsubscribe({searchParams}:{searchParams:Promise<{token?:string}>}){const token=(await searchParams).token;let removed=false;if(token&&/^[a-f0-9-]{36}$/.test(token)){const supabase=await createClient();const {data}=await supabase.rpc("unsubscribe_from_trip",{p_token:token});removed=Boolean(data);}return <main className="readable-surface page-shell my-16 max-w-2xl py-12 text-center"><h1 className="font-serif text-4xl font-semibold text-forest">{removed?"You’re unsubscribed":"Unsubscribe link unavailable"}</h1><p className="mt-4 text-stone-600">{removed?"Email and text journal updates have been turned off.":"This unsubscribe link could not be used."}</p><Link href="/" className="button-primary mt-7">Return home</Link></main>}
