const fields = ["Title", "Slug", "Entry date", "Location name", "Latitude", "Longitude", "Trip day", "Mileage to date", "Notification title"];
export function EditorPreview({ edit = false }: { edit?: boolean }) { return <div className="mt-10 space-y-6"><div className="grid gap-5 rounded-3xl bg-white p-6 shadow-sm sm:grid-cols-2">{fields.map((field) => <label key={field} className="form-label">{field}<input disabled className="form-input mt-2 bg-stone-50" placeholder={field} /></label>)}<label className="form-label sm:col-span-2">Body<textarea disabled className="form-input mt-2 min-h-40 bg-stone-50" placeholder="Write the day’s story…" /></label><div className="rounded-2xl border border-dashed border-stone-300 p-7 text-center text-sm text-stone-500 sm:col-span-2">Cover photo and multiple-photo uploader with captions</div><div className="rounded-2xl bg-[#ebe6da] p-5 sm:col-span-2"><p className="font-bold text-forest">Location tools</p><p className="mt-1 text-sm text-stone-600">Use current location · Search a place · Drop a pin on the map</p></div><div className="space-y-3 sm:col-span-2">{["Send notification on publish", "Update completed-route checkpoint"].map((label) => <label key={label} className="flex items-center gap-3 text-sm font-semibold text-stone-700"><input type="checkbox" disabled className="h-5 w-5" />{label}</label>)}</div><div className="flex flex-wrap gap-3 sm:col-span-2"><button disabled className="button-primary opacity-50">{edit ? "Save changes" : "Save post"}</button><button disabled className="rounded-full border border-stone-300 px-5 py-3 text-sm font-bold text-stone-500">Draft / Published</button></div></div></div>; }
